package model;

public class Admin extends Account {

    public Admin(String firstName, String lastName, String emailAddress, int phoneNumber, String username,
                 String password) {
        super(firstName, lastName, emailAddress, phoneNumber, username, password);
    }
}
